# PR 76: review refinements and qualification

Date: 2026-09-25. Reviewed PR head: `97927d763103da6418b154f3ecf3a9b9bf690bf2`.
Runtime/test candidate: `d262aa23d19e191129399ec6ad3fb080e96f0bd7`.

This addendum records the review requested after the initial harness audit. The
original audit and design remain historical evidence, not a claim that every
follow-on proposal was implemented. Production was not redeployed, restarted,
or reconfigured. No new MCP tools, tool schemas, grant authority, provider
configuration, spending defaults, or state migrations were introduced.

## Findings resolved

| Finding | Refinement | Regression coverage |
| --- | --- | --- |
| A terminal resume or late approval could request a needless driver. Parked runs could be converted to failures by stale driver initialization. | Gate service launch on the committed ready state; re-read state under the launch lease; return before initialization for non-runnable records. | Completed/cancelled/failed resume leaves no driver result or ownership file; paused/uncertain/approval-wait records and events remain unchanged. |
| A pause winning the admission re-read still allowed the model or effect callback and budget reservation. | Treat accepted pause/control as an admission barrier and inspect the returned state before either callback. In-flight result publication keeps its prior semantics. | Deterministic SQLite interleavings for model and tool admission; unchanged winning revision and no callback. |
| Recovery of a pending operation could lose the fact that its external outcome was unknown, or make pause look blindly resumable. | Persist unknown outcome on pending recovery; paused pending work stays uncertain and requires reconciliation. | Model/tool pending phases under pause/cancel; no callback and no blind resume. |
| An owning driver failure could override persisted operator control. | Preserve cancel/pause intent in failure publication; keep pending external outcomes explicit; publish only for currently runnable state while holding existing ownership/CAS protections. | Cancel/pause crossed with ready/pending phases. |
| Chat error envelopes before DONE could be treated as empty chunks; an unfinished trailing Responses frame retained native executable output. | Reject error envelopes before finalization and clear both normalized calls and native output on interrupted finalization. | Both error envelope forms and incomplete trailing frame; repeated finish remains stable. |

## Negative-control method

The expanded suite was first compiled against the unchanged reviewed runtime.
All 18 existing scenarios passed and all 19 additional scenarios failed. The
new tests were committed separately as `1af6425` before the runtime fixes.
The corrected candidate passes all 37 scenarios. These are 19 new regression
scenarios across the finding groups above, not 19 unrelated root causes.

The test-only executable recognizes `--agent-runner` and exits immediately so
that a deliberately reproduced unwanted launch cannot recursively run the test
suite. This guard does not modify the production driver or grant enforcement.

## Qualification

| Check | Observed result |
| --- | --- |
| Negative control on reviewed runtime | 18 existing pass; 19 new scenarios fail, as expected |
| Corrected harness regressions | 37/37 pass |
| Selected native CTest suites | 35/35 pass |
| Repeated harness/runs/providers suites | 20 repetitions each; 60 successful invocations |
| Runtime contract and frozen schema parity | Both pass |
| Engine, agent reliability and backend harness SDK smoke tests | All three pass |

All eight candidate stages exited 0. Local qualification completed at
`2026-09-25T12:37:14.4539010Z`; the validated runtime SHA is recorded above.

Machine-readable provenance, arguments, exit codes, durations and binary hash
are retained in [the validation manifest](audit-evidence/2026-09-25-pr76-review/validation.json).
The evidence logs distinguish the intentionally failing negative control from
the passing candidate. Local Windows qualification uses the existing Release
build configuration with LTO and TUI disabled; it is not a replacement for the
repository's complete hosted platform and release-certification gates.

Four local native tests (`computer-native`, `computer-broker-native`, `capture`,
`setup`) are deliberately excluded from this development run. Their omission
is explicit, not counted as a pass. Hosted qualification and physical-device
certification remain separate scopes.

The isolated fixture ran in a uniquely named, bounded, no-trigger temporary
Windows task so its independent state coordinator did not inherit the generic
execution job's lifetime. The task was collected and removed after completion;
no unrelated process, scheduler setting, or service was stopped.

## Merge and deployment boundary

Merge only the reviewed final head after all required hosted checks succeed,
including complete C++ certification. Use a head-commit match and retain branch
protections. The final CI and merge receipts belong to the PR review thread;
this document does not claim a future CI result or a production deployment.

A complete concurrency specification (including final-answer completion versus
cancellation), typed tool-argument contracts, bounded model-readable artifacts,
resource accounting and independent live-task quality benchmarks remain the
follow-on proposals in the original design. This review claims improved tested
invariants, not measured performance gains or a live-model benchmark score.
